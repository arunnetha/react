# This workflow will do a clean installation of node dependencies, cache/restore them, build the source code and run tests across different versions of node
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-nodejs

name: Node.js CI

on:
  push:
    branches: [ "origin/dev" ]
  pull_request:
    branches: [ "uat", "main" ]
  workflow_dispatch:

jobs:
  Build_dev:
    name: Build Development
    if: github.event_name == 'push' && github.head_ref == 'origin/dev'
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [12.x]
    environment: 
      name: Development
    steps:
      - name: dev build
        uses: actions/checkout@v2
      - run: ls
  Build_uat:
    name: Build Development
    if: github.event_name == 'pull_request' && github.head_ref == 'uat'
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [12.x]
    environment: 
      name: Development
    steps:
      - name: dev build
        uses: actions/checkout@v2
      - run: ls